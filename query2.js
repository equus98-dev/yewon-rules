const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function run() { const rule = await prisma.rule.findFirst({ where: { title: { contains: '정관' } }, include: { currentRevision: { include: { articles: { orderBy: { articleNumber: 'asc' } } } } } }); const addendums = rule.currentRevision.articles.filter(a => a.articleNumber >= 8000); console.log(addendums.map(a => ({ id: a.id, title: a.title, text: a.contentText.substring(0, 30) }))); } run();
